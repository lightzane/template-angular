import { MyHighlightOnHoverDirective } from './my-highlight-on-hover.directive';

describe('MyHighlightOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new MyHighlightOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
